# Cursor Rule Example

`holdthegoblin wrap --agent cursor .` writes a project rule to:

```text
.cursor/rules/holdthegoblin.mdc
```

Cursor project rules are guidance, not a hard sandbox. Use Claude Code hooks or `holdthegoblin verify` in CI when you need hard blocking.

