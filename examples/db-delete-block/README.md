# DB Delete Block Demo

This demo runs the same Claude Code hook entrypoint that a real project uses.

```bash
npm run demo:db-delete
```

Expected result: HoldTheGoblin returns a Claude-compatible JSON response with `permissionDecision: "deny"` for a destructive database command.

